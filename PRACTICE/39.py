# valid python code

# lambda a,b: a if a<b else b
# lambda a,b: True

# invalid python code 

# lambda (a,b): return a if a<b else b # lambda 자체가 return을 포함하고 있기 때문에 써줄 필요가 없고, () 또한 써주지 않는다. 맞는 답) lambda a,b: a if a<b else b
# lambda a,b = a if a<b else b # = 대신 :를  써줘야 한다. 맞는 답) lambda a,b: a if a<b else
